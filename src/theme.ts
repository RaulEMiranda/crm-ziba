import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4A90E2',  // Color principal
    },
    secondary: {
      main: '#ababab',  // Color secundario
    },
  },
});

export default theme;
